# More experimentation with the 'enumerate' function.

# You can iterate around a for loop with more than one value by specifying \
# both when setting up the for loop.
for index, char in enumerate("abcdefgh"):
    print(index, char)

# See 'index_method.py' for more examples using 'enumerate'.
